#include "disjointset.h"

// void Initialize(DisjointSet set, int number) {
//     set.parent = malloc(number * sizeof(size_t));
//     for (int i = 0; i < count; i++) {
//         set.parent[i] = SIZE_MAX;
//     }
// }

// void DisposeDisjointSet(DisjointSet set) {
//     free(set.parent);
// }

// int Find(size_t element, DisjointSet set) {
//     for (; set.parent[element] != SIZE_MAX; element = set.parent[element]);
//     return element;
// }

// int FindReplace(size_t element, DisjointSet set, size_t value) {
//     for (; set.parent[element] != SIZE_MAX; element = originalValue) {
//         size_t originalValue = set.parent[element];
//         set.parent[element] = value;
//     }
//     set.parent[element]
//     return element;
// }

// void Union(size_t element1, size_t element2, DisjointSet set) {
    
// }

